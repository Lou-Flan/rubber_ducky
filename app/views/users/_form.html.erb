<%= form_with(model: @user, local: true) do |form| %>
  <div>
    <%= form.label :bio %><br>
    <%= form.text_area :bio %><br>
    <%= form.label :avatar %> <br>
    <%= form.file_field :avatar, accept:
  'image/png,image/gif,image/jpeg,image/jpg' %>
  </div>
  <%= form.submit %>
</div>
<% end %>
